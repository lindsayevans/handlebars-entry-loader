language: node_js
cache: yarn
deploy:
  - provider: npm
    on:
      tags: true
    skip_cleanup: true
    email: $NPM_EMAIL
    api_key: $NPM_API_KEY
  - provider: npm
    on:
      branch: develop
      tags: next
    skip_cleanup: true
    email: $NPM_EMAIL
    api_key: $NPM_API_KEY